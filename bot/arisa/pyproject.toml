[project]
name = "autoworker-bot"
version = "0.1.0"
description = "오토봇 + 카페봇 (Arisa2 / KakaoTalk)"
requires-python = ">=3.11"
dependencies = [
    "airi",
]

[project.scripts]
autoworker-bot = "autoworker.__main__:main"

[tool.uv.sources]
airi = { git = "https://github.com/ye-seola/arisa2.git", subdirectory = "python", branch = "main" }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/autoworker"]
